# Local wrangler/Pages cache — machine state, not site content.
.wrangler/

# Local QA/browser tool logs — never site content.
.gstack/

# Python bytecode from local test runs — never site content.
__pycache__/
